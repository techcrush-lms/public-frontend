'use client';

import NotFoundContent from '@/app/(components)/NotFoundContent'; // the part that uses useSearchParams or useRouter

export default function NotFoundPage() {
  return <NotFoundContent />;
}
